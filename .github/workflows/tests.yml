name: tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  include-what-you-must:
    uses: ZIMO-Elektronik/.github-workflows/.github/workflows/x86_64-linux-gnu-gcc.yml@v0.3.0
    with:
      args: -DCMAKE_BUILD_TYPE=Debug
      target: MM2IncludeWhatYouMust
